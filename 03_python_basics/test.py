## Indentation
## Python uses indentation to define blocks of code. Consistent use of spaces (commonly 4) or a tab is required.

age=25

if age>30:              # 25 > 30 false
    print("the age is",age)          # if true execute this line
print("outside the if block the age is: ",age)              # this will also get executed
