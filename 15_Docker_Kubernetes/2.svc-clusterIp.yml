apiVersion: v1
kind: Service
metadata:
  name: flask-service
spec:
  selector:
    app: flask-web
  ports:
    - protocol: TCP
      port: 80 # wrt host
      targetPort: 5000 # wrt to container
  type: ClusterIP   # 👈